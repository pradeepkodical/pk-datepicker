import { createTheme } from '@mui/material';

export const defaultTheme = createTheme({
  palette: {
    primary: {
      light: '#e2e0eb',
      main: '#2b216e',
    },
    action: {
      selected: '#bebcd2',
    },
  },
});
